# Supabase 프로젝트 설정 > API 에서 확인한다.
# anon 키는 브라우저에 노출되어도 되는 값이다. Row Level Security 가 데이터를 지킨다.
# service_role 키는 RLS 를 통째로 우회하므로 절대 NEXT_PUBLIC_ 으로 두면 안 된다.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
